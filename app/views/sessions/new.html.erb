<h1 align = "center"> Login form</h1>
 <div class="container">
	<div class="row">
		<div class="col-md-4 mx-auto">
			<%= form_for( :session, :html => {class: " myform form",role: "form"}, url: login_path ) do |f| %>
				<div class="form-group">
					<%= f.email_field :email, class: "form-control my-input", placeholder: "Email" %>
				</div>
				<div class="form-group">
					<%= f.password_field :password,class: "form-control my-input", placeholder: "Password", autocomplete: "off" %>
				</div>
				<div class="text-center ">
					<%= f.submit "Log in", class: " btn btn-block send-button tx-tfm" %> 
				</div>
				<p class="small mt-3">By signing up, you are indicating that you have read and agree to the <a href="#" class="ps-hero__content__link">Terms of Use</a> and <a href="#">Privacy Policy</a>.
				</p>
			<% end%>
		</div>
	</div>
</div>
